// Use === for Comparison

let a = 5;
let b = "5";

console.log(a === b); //false
console.log(a == b); //true

// Note: Use === instead of == for comparisons to avoid type coercion and ensure accurate comparisons.