// 📌 1. Limit to only first 2 documents
db.animeCharacters.find().limit(2)

// 📌 2. Sort by name in Ascending Order
db.animeCharacters.find({}, { "name": 1, _id: 0 }).sort({ "name": 1 })

// 📌 3. Sort by name in Descending Order
db.animeCharacters.find({}, { "name": 1, _id: 0 }).sort({ "name": -1 })

// 📌 4. Sort by name in Ascending Order and Skip 1 document
db.animeCharacters.find({}, { "name": 1, _id: 0 }).sort({ "name": 1 }).skip(1)
